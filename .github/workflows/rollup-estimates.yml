name: Rollup Estimates and Remaining Work

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Task/Story number that was updated (triggers bottom-up rollup to parents)'
        required: true
        type: number
  issues:
    types: [edited, labeled]

jobs:
  rollup-to-parents:
    # Only run if manually triggered OR if 'sync' label was added
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issues' && github.event.action == 'labeled' && github.event.label.name == 'sync')
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
      repository-projects: write
      # Note: GITHUB_TOKEN cannot access organization-level projects
      # Organization projects require elevated permissions

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Rollup Estimates to Parent Issues
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PAT_WITH_PROJECT_ACCESS || secrets.GITHUB_TOKEN }}
          script: |
            console.log('🔄 Rollup workflow triggered');
            console.log('=====================================');

            // Determine issue number based on trigger type
            let issueNumber;
            if (context.eventName === 'workflow_dispatch') {
              // Manual trigger
              issueNumber = parseInt(context.payload.inputs.issue_number);
              console.log(`📋 Manual trigger for issue #${issueNumber}`);
            } else if (context.eventName === 'issues') {
              // Issue event trigger
              issueNumber = context.issue.number;
              console.log(`📋 Issue event trigger for issue #${issueNumber}`);
            } else {
              console.log('❌ Unknown trigger type');
              return;
            }

            // Check if triggered by sync label
            if (context.eventName === 'issues' && context.payload.action === 'labeled') {
              if (context.payload.label.name !== 'sync') {
                console.log(`⏭️  Skipping - label "${context.payload.label.name}" is not "sync"`);
                return;
              }
              console.log('✅ Triggered by "sync" label');
            }


            // Schedule label removal at start (runs async, happens after 5 seconds)
            const removeLabelPromise = (async () => {
              if (context.eventName === 'issues' && context.payload.action === 'labeled' && context.payload.label.name === 'sync') {
                // Wait to ensure workflow has time to process
                await new Promise(r => setTimeout(r, 5000));
                try {
                  await github.rest.issues.removeLabel({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    issue_number: issueNumber,
                    name: 'sync'
                  });
                  console.log('✅ Removed "sync" label - workflow can be re-triggered');
                } catch (error) {
                  console.log('ℹ️  Could not remove sync label:', error.message);
                }
              }
            })();

            // Get issue details
            const issue = await github.rest.issues.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber
            });

            const issueBody = issue.data.body || '';

            // Helper function to find parent using native sub-issues
            async function findParentIssue(issueNum) {
              console.log(`Looking for parent of issue #${issueNum}...`);
              
              // Method 1: Check GitHub's native sub-issues
              try {
                const graphqlQuery = `
                  query($owner: String!, $repo: String!, $issueNumber: Int!) {
                    repository(owner: $owner, name: $repo) {
                      issue(number: $issueNumber) {
                        trackedInIssues(first: 1) {
                          nodes {
                            number
                          }
                        }
                      }
                    }
                  }
                `;
                const result = await github.graphql(graphqlQuery, {
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issueNumber: issueNum
                });
                const parent = result.repository.issue.trackedInIssues.nodes[0];
                if (parent) {
                  console.log(`Parent found via sub-issues: #${parent.number}`);
                  return parent.number;
                }
              } catch (error) {
                console.log(`Could not query sub-issues: ${error.message}`);
              }
              
              // Method 2: Fall back to description parsing
              const issue = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNum
              });
              const body = issue.data.body || '';
              const bodyWithoutComments = body.replace(/<!--[\s\S]*?-->/g, '');
              const parentMatch = bodyWithoutComments.match(/(?:Epic|Feature|Parent Feature|Parent Epic):\s*#(\d+)/i);
              if (parentMatch) {
                const parentNum = parseInt(parentMatch[1]);
                console.log(`Parent found via description: #${parentNum}`);
                return parentNum;
              }
              return null;
            }

            // Helper function to find children using native sub-issues
            async function findChildIssues(parentNum) {
              console.log(`Finding children of issue #${parentNum}...`);
              
              // Method 1: Check GitHub's native sub-issues using REST API
              try {
                const response = await github.request('GET /repos/{owner}/{repo}/issues/{issue_number}/sub_issues', {
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: parentNum
                });

                if (response.data && response.data.length > 0) {
                  console.log(`Found ${response.data.length} children via REST API sub-issues`);
                  return response.data.map(c => c.number);
                }
              } catch (error) {
                console.log(`Could not get sub-issues from REST API: ${error.message}`);
              }
              
              // Method 2: Fall back to description parsing
              console.log('Falling back to description parsing...');
              const allIssues = [];
              let page = 1;
              let hasMore = true;
              while (hasMore) {
                const issues = await github.rest.issues.listForRepo({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  state: 'all',
                  per_page: 100,
                  page: page
                });
                if (issues.data.length === 0) {
                  hasMore = false;
                } else {
                  allIssues.push(...issues.data);
                  page++;
                }
              }
              const children = allIssues.filter(issue => {
                const body = issue.body || '';
                const parentPattern = new RegExp(`(?:Epic|Feature|Parent Feature|Parent Epic):\\s*#${parentNum}(?:\\s|$)`, 'i');
                return parentPattern.test(body);
              });
              console.log(`Found ${children.length} children via description parsing`);
              return children.map(c => c.number);
            }

            console.log(`📌 Processing: #${issueNumber} - ${issue.data.title}`);
            console.log(`📊 Workflow: CHILD → PARENT rollup (bottom-up)`);
            console.log('=====================================\n');

            // Find parent issue
            const parentIssueNumber = await findParentIssue(issueNumber);

            if (!parentIssueNumber) {
              console.log('No parent issue reference found');
              console.log('This issue is either:');
              console.log('  - A top-level Epic (no parent needed)');
              console.log('  - Missing a parent reference');
              return;
            }

            // Function to get all project fields for an issue
            async function getProjectFields(issueNumber) {
              const issue = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber
              });

              // Get all projects and their items (organization-level projects)
              const projectQuery = `
                query($owner: String!) {
                  organization(login: $owner) {
                    projectsV2(first: 10) {
                      nodes {
                        id
                        title
                        items(first: 100) {
                          nodes {
                            id
                            content {
                              ... on Issue {
                                id
                                number
                              }
                            }
                            fieldValues(first: 20) {
                              nodes {
                                ... on ProjectV2ItemFieldNumberValue {
                                  number
                                  field {
                                    ... on ProjectV2Field {
                                      id
                                      name
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                        fields(first: 20) {
                          nodes {
                            ... on ProjectV2Field {
                              id
                              name
                              dataType
                            }
                          }
                        }
                      }
                    }
                  }
                }
              `;

              const projectResult = await github.graphql(projectQuery, {
                owner: context.repo.owner
              });

              const projects = projectResult.organization.projectsV2.nodes;

              // Find the specific project "[TEMPLATE] EngageMe"
              const project = projects.find(p => p.title === '[TEMPLATE] EngageMe');

              if (!project) {
                console.log(`⚠️  Issue #${issueNumber} - [TEMPLATE] EngageMe project not found`);
                return null;
              }

              // Find the item for this issue
              const item = project.items.nodes.find(i =>
                i.content && i.content.number === issueNumber
              );

              if (item) {
                const fields = {};
                item.fieldValues.nodes.forEach(fv => {
                  if (fv.field && fv.number !== undefined) {
                    fields[fv.field.name] = {
                      value: fv.number,
                      fieldId: fv.field.id
                    };
                  }
                });

                return {
                  projectId: project.id,
                  itemId: item.id,
                  fields: fields,
                  allFields: project.fields.nodes
                };
              }

              return null;
            }

            // Function to update project field
            async function updateProjectField(projectId, itemId, fieldId, value) {
              const mutation = `
                mutation($projectId: ID!, $itemId: ID!, $fieldId: ID!, $value: Float!) {
                  updateProjectV2ItemFieldValue(input: {
                    projectId: $projectId
                    itemId: $itemId
                    fieldId: $fieldId
                    value: {
                      number: $value
                    }
                  }) {
                    projectV2Item {
                      id
                    }
                  }
                }
              `;

              await github.graphql(mutation, {
                projectId: projectId,
                itemId: itemId,
                fieldId: fieldId,
                value: value
              });
            }

            // Function to find all children and sum their estimates and remaining work
            async function calculateChildrenSums(parentNumber) {
              const childNumbers = await findChildIssues(parentNumber);
              console.log(`Found ${childNumbers.length} child issue(s)`);

              let totalEstimate = 0;
              let totalRemaining = 0;
              const childDetails = [];

              for (const childNumber of childNumbers) {
                const projectFields = await getProjectFields(childNumber);

                if (projectFields && projectFields.fields) {
                  const estimate = projectFields.fields['Estimate']?.value || 0;
                  const remaining = projectFields.fields['Remaining']?.value || 0;

                  totalEstimate += estimate;
                  totalRemaining += remaining;

                  childDetails.push({
                    number: childNumber,
                    estimate: estimate,
                    remaining: remaining
                  });

                  console.log(`  #${childNumber}: Estimate=${estimate}, Remaining=${remaining}`);
                }
              }

              return {
                totalEstimate,
                totalRemaining,
                childCount: childNumbers.length,
                childDetails
              };
            }

            // Function to recursively update parent chain
            async function updateParentChain(parentNumber, depth = 0) {
              const indent = '  '.repeat(depth);
              console.log(`${indent}📊 Updating parent #${parentNumber}...`);

              // Check if parent issue exists
              try {
                await github.rest.issues.get({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: parentNumber
                });
              } catch (error) {
                console.error(`${indent}❌ Parent issue #${parentNumber} not found (404)`);
                return;
              }

              // Calculate sums from all children
              const sums = await calculateChildrenSums(parentNumber);

              console.log(`${indent}Total from ${sums.childCount} children: Estimate=${sums.totalEstimate}, Remaining=${sums.totalRemaining}`);

              // Get parent's project fields
              const parentProjectFields = await getProjectFields(parentNumber);

              if (!parentProjectFields) {
                console.log(`${indent}⚠️  Parent #${parentNumber} not found in any project`);
                return;
              }

              // Find Estimate and Remaining field IDs
              const estimateField = parentProjectFields.allFields.find(f => f.name === 'Estimate');
              const remainingField = parentProjectFields.allFields.find(f => f.name === 'Remaining');

              if (!estimateField || !remainingField) {
                console.log(`${indent}⚠️  Estimate or Remaining fields not found in project`);
                return;
              }

              // Update parent's fields
              try {
                await updateProjectField(
                  parentProjectFields.projectId,
                  parentProjectFields.itemId,
                  estimateField.id,
                  sums.totalEstimate
                );
                console.log(`${indent}✅ Updated Estimate to ${sums.totalEstimate}`);

                await updateProjectField(
                  parentProjectFields.projectId,
                  parentProjectFields.itemId,
                  remainingField.id,
                  sums.totalRemaining
                );
                console.log(`${indent}✅ Updated Remaining to ${sums.totalRemaining}`);


              } catch (error) {
                console.error(`${indent}Error updating parent:`, error.message);
                return;
              }

              // Find grandparent and continue chain
              const grandparentNumber = await findParentIssue(parentNumber);

              if (grandparentNumber) {
                console.log(`${indent}Found grandparent #${grandparentNumber}, continuing chain...`);
                await updateParentChain(grandparentNumber, depth + 1);
              } else {
                console.log(`${indent}No grandparent found, rollup complete`);
              }
              }
            }

            // Start the rollup chain
            await updateParentChain(parentIssueNumber);

            console.log('✅ Rollup complete!');

